# editor / OS cruft
*~
*.swp
.DS_Store
# local backups from on-device edits
*.orig
*.bak
*.stock.bak
*.2011.orig
# scratch
/tmp/

# --- messaging/: vendored native libpurple plugin build artifacts ---
# Source is vendored; built objects/libs are rebuilt via each plugin's build step.
build/
build-arm/
build-native/
*.o
*.a
*.la
*.lo
*.so
*.so.*
*.dll
CMakeCache.txt
CMakeFiles/
*.cmake.stamp
# libqrencode in-tree build dirs
messaging/*/plugin/libqrencode/inst/
messaging/*/plugin/libqrencode/use/

# --- prebuilt cross-deps (large binaries; not committed) ---
/build-output/
messaging/libpurple/

# Rust cross-build artifacts (purple-presage)
target/

# connector test harness — real credentials, never commit
tests/accounts.local.tsv

# Cross-build compile database (generated by build.sh)
messaging/imlibpurpleservice/compile_commands.json
/messaging/telegram/plugin/libtgvoip/install/

# libtgvoip in-tree autotools/libtool build output (the 763 source files are tracked; everything
# below is generated by ./configure && make - do not commit). config.h is anchored to the TOP
# level only: webrtc_dsp/absl/base/config.h and audio_processing/include/config.h are real tracked
# source headers, not the generated one.
/messaging/telegram/plugin/libtgvoip/Makefile
/messaging/telegram/plugin/libtgvoip/config.h
/messaging/telegram/plugin/libtgvoip/config.log
/messaging/telegram/plugin/libtgvoip/config.status
/messaging/telegram/plugin/libtgvoip/stamp-h1
/messaging/telegram/plugin/libtgvoip/libtool
/messaging/telegram/plugin/libtgvoip/**/.deps/
/messaging/telegram/plugin/libtgvoip/**/.libs/
/messaging/telegram/plugin/libtgvoip/**/.dirstamp
/messaging/telegram/plugin/libtgvoip/**/*.lo
/messaging/telegram/plugin/libtgvoip/**/*.o
/messaging/telegram/plugin/libtgvoip/**/*.la
