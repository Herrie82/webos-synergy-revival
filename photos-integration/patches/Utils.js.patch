--- a/photos-src/base/Utils.js
+++ b/photos-src/base/Utils.js
@@ -415,6 +415,27 @@
 
 							break;
 
+
+							// Synergy-revival cloud photo sources. Every one has the SAME capability
+							// shape and keeps listAlbums/listPhotos in its own already-bus-registered
+							// service, so they share one handler instead of a copy-pasted case each.
+							// serviceName derives from the templateId (com.palm.X -> com.palm.service.X);
+							// to add a provider, add its templateId to the fall-through list below.
+							case "com.palm.dropbox":
+							case "com.palm.boxnet":
+							case "com.palm.onedrive":
+							case "com.palm.pcloud":
+							case "com.palm.flickr":
+							case "com.palm.kdrive":
+							case "com.palm.yandexdisk":
+								accountsCapability[account.templateId].getComments      =false;
+								accountsCapability[account.templateId].updateComments   =false;
+								accountsCapability[account.templateId].getUserInfo      =false;
+								accountsCapability[account.templateId].videoDownload    =false;
+								accountsCapability[account.templateId].serviceName      =
+									account.templateId.replace(/^com\.palm\./, "com.palm.service.");
+
+							break;
 							default:
 								accountsCapability[account.templateId]=new Capability();
 							break;
