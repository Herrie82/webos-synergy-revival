--- a/stylesheets/app.css
+++ b/stylesheets/app.css
@@ -271,16 +271,24 @@
 	background: url(../images/list-view-avatar-frame.png) left top no-repeat;
 }
 .unread-count {
-	padding:0 6px;
+	padding:0 9px;
 	color: #f2f2f2;
 	text-align: center;
 	vertical-align: middle;
+	/* Pin Prelude: the badge number was inheriting a fallback face and reading as a different font. */
+	font-family: Prelude, 'Prelude Medium', 'Segoe UI', Arial, Helvetica, sans-serif;
 	font-size: 15px;
 	line-height: 23px;
 	height: 24px;
-	width:24px;
+	/* Grow the badge with its content: the stock fixed 24px list-unread.png circle clipped 2-3 digit
+	 * counts. Auto width + min-width keeps single digits circular. The stock badge was a translucent
+	 * black circle (~26% over the light list), so match that with rgba rather than a solid grey - a
+	 * solid #8f8f8f read noticeably darker than the original. */
+	width: auto;
+	min-width: 24px;
+	-webkit-box-sizing: border-box;
 	-webkit-border-radius: 12px;
-	background: url(../images/list-unread.png) center center no-repeat;
+	background: rgba(0, 0, 0, 0.26);
 }
 
 .favorite-icon{
